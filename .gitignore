# 内部设计文档不公开。
# 注意：必须写成 docs/* 而不是 docs/ —— 目录整体被排除时，
# 后面的 ! 放行规则不会生效。
docs/*
!docs/oauth-setup.md

# deps / build
node_modules/
dist/
*.tgz

# spike 产物：含真实令牌与未脱敏邮件数据，绝不入库
spikes/.secrets.json
spikes/out/

# 本地数据目录（若在仓库内测试）
.zmail/
.zmail-*/

# 杂项
.DS_Store
*.log

.env*